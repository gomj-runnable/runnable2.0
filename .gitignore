# Nuxt dev/build outputs
.output
.data
.nuxt
# PGlite local DB files
.data/pglite/
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Environment-specific files (local/prod)
# *.local.* 및 *.prod.* 패턴의 파일은 환경별 민감 정보를 포함할 수 있으므로 추적하지 않는다.
# 예: configmap.local.yaml, secret.prod.yaml, README.local.md
*.local.*
*.prod.*
*.dev.*
**/secret*.yaml
!*.example.*
!.env.*.example

# Static uploads & MCP
public/uploads/
# GIS 데이터 (~397MB), 변환기(scripts/convert-crosswalk.mjs) 입력용
public/crosswalk/
.mcp.json

# Claude Code 로컬 아티팩트 (스크린샷, 런타임 lock 등)
.claude/*.png
.claude/scheduled_tasks.lock

# Oh My ClaudeCode (omc) 플러그인 런타임 state — 루트/서브디렉터리 모두 무시
.omc/

# TODO
*.TODO.md

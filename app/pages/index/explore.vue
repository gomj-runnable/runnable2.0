<script setup lang="ts">
/**
 * 탐색 탭 panel (path: `/explore`).
 * 탐색 패널 자체는 우측 플러그인 surface(PluginSurfaceHost)가 렌더하고,
 * surface 개폐는 useTabRoute(applyTab)가 path 동기화로 담당하므로 좌측 영역엔 표시가 없다.
 */
</script>

<template>
    <div class="hidden" aria-hidden="true" />
</template>
